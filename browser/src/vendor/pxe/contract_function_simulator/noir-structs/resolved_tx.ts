// BEGIN VENDORED-PROVENANCE — generated by `just vendor-headers` (tools/provenance.py); do not hand-edit.
// VENDORED — not our code. Re-vendor rather than editing here.
//   upstream-repo:   AztecProtocol/aztec-packages
//   upstream-path:   yarn-project/pxe/src/contract_function_simulator/noir-structs/resolved_tx.ts
//   upstream-commit: 233d8e099336c1773b89e939100af047ed9c4f71
//   licence:         Apache-2.0
//   local-edits:     none
//   inventory:       REUSE-INVENTORY.md RI-97
// END VENDORED-PROVENANCE

import type { Fr } from '@aztec/foundation/curves/bn254';
import type { TxHash } from '@aztec/stdlib/tx';

/**
 * The resolved on-chain context of a transaction.
 *
 * Carries the note hashes and first nullifier needed to discover notes that originated from the transaction, plus the
 * number and hash of the block in which it was mined.
 *
 * A TS version of the `ResolvedTx` struct in `oracle/tx_resolution.nr`; its wire layout lives in the `RESOLVED_TX`
 * type mapping.
 */
export type ResolvedTx = {
  txHash: TxHash;
  uniqueNoteHashesInTx: Fr[];
  firstNullifierInTx: Fr;
  blockNumber: number;
  blockHash: Fr;
};
